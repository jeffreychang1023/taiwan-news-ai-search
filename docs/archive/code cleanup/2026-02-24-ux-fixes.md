# UX Fixes Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Fix 11 UX issues identified in `docs/0224UX.md` through 5 targeted changes: QueryUnderstanding unification, frontend state cleanup, language guardrail, time gap prompt, and Analyst date injection.

**Architecture:** Consolidate scattered query analysis (3 pre-retrieval modules + qdrant.py hardcoded lists) into a single `QueryUnderstanding` module with regex fast path + LLM fallback. Fix frontend state leaks. Add prompt-level guardrails for language and temporal awareness.

**Tech Stack:** Python 3.11, aiohttp, Qdrant, PromptRunner pattern (prompts.xml), vanilla JS frontend

---

## Context for Implementer

### Key Files Map

| Area | File | Purpose |
|------|------|---------|
| Prompt config | `config/prompts.xml` | All LLM prompts (XML format, PromptRunner loads these) |
| Prompt engine | `core/prompts.py` | `PromptRunner` base class, `fill_prompt()`, variable resolution |
| Pre-retrieval | `core/baseHandler.py:350-394` | `prepare()` — launches pre-check tasks via `asyncio.gather` |
| Query rewrite | `core/query_analysis/query_rewrite.py` | Current QueryRewrite (to be replaced) |
| Time extractor | `core/query_analysis/time_range_extractor.py` | Current TimeRangeExtractor (to be replaced) |
| Author detector | `core/query_analysis/author_intent_detector.py` | Current AuthorIntentDetector (to be replaced) |
| Retrieval | `retrieval_providers/qdrant.py:884-1137` | Hardcoded domain filter + temporal keywords (to be refactored) |
| Post-ranking | `core/post_ranking.py:30-120` | SummarizeResults (prompt needs temporal context) |
| Analyst prompt | `reasoning/prompts/analyst.py:300-474` | `_build_base_research_prompt()` (needs current_date) |
| Frontend | `static/news-search.js` | Search UI, state management |

### PromptRunner Pattern

All prompts live in `config/prompts.xml`. To add a new prompt:

```xml
<Prompt ref="MyPromptName">
  <promptString>
    Instructions here. Variables: {request.query}, {system.current_date}
  </promptString>
  <returnStruc>
    {"field": "description"}
  </returnStruc>
</Prompt>
```

Call via: `response = await self.run_prompt("MyPromptName", level="low")`

Available variables (resolved in `core/prompts.py:get_prompt_variable_value()`):
- `{request.query}` — decontextualized query
- `{request.rawQuery}` — original query
- `{system.current_date}` — today's date (YYYY-MM-DD)
- `{system.temporal_range}` — "User asked for news from X to Y" (if temporal)
- `{request.answers}` — ranked results (for post-ranking prompts)

### Handler Attribute Contracts (backward compatibility)

These attributes are READ by downstream code. New modules MUST set them in the same format:

| Attribute | Format | Readers |
|-----------|--------|---------|
| `handler.rewritten_queries` | `list[str]` (empty = no expansion) | `baseHandler.py:506` |
| `handler.temporal_range` | `dict` with keys: `is_temporal`, `start_date`, `end_date`, `method`, `confidence`, `relative_days` | `baseHandler.py:453-489`, `prompts.py:151-164`, `deep_research.py:221-240` |
| `handler.author_search` | `dict` with keys: `is_author_search`, `author_name`, `pattern_matched` | `baseHandler.py:492-498`, `baseHandler.py:583-594` |

### Running the Server

```bash
cd /c/users/user/nlweb/code/python
python -m webserver.aiohttp_server
```

Server runs on port 8080. Frontend at `http://localhost:8080`.

---

## Batch 1: Quick Prompt Fixes (T5 + T3 + T4)

These are independent, small changes. Do all three, then verify together.

### Task 1: Analyst prompt — inject current_date (T5)

**Files:**
- Modify: `code/python/reasoning/prompts/analyst.py:300-474`

**Resolves:** Issue #3 (year misinterpretation in deep research)

**Step 1: Read the file and locate `_build_base_research_prompt()`**

Read `reasoning/prompts/analyst.py` lines 300-340 to see the current method start.

**Step 2: Add current_date injection**

At the start of `_build_base_research_prompt()`, after building the initial prompt parts, add temporal grounding:

```python
from datetime import datetime

# In _build_base_research_prompt(), after the existing prompt header section,
# add before the context section:

current_date = datetime.now().strftime("%Y-%m-%d")

# Add to the prompt string:
temporal_grounding = f"""
**重要時間資訊**：
- 今天的日期：{current_date}
- 新聞報導只可能來自過去，不可能來自未來
- 若用戶提到月份但未指定年份，預設為最近的「過去」該月份
  （例：現在是 {current_date[:4]} 年，「12月」= {int(current_date[:4])-1} 年 12 月，不是未來的 12 月）
"""
```

Insert this block into the prompt string, right after the initial system instruction and before the source materials section.

**Step 3: Verify**

```bash
cd /c/users/user/nlweb/code/python
python -c "from reasoning.prompts.analyst import AnalystPromptBuilder; b = AnalystPromptBuilder(); p = b.build_research_prompt(query='test', formatted_context='ctx', mode='discovery'); assert '今天的日期' in p; print('OK: current_date injected')"
```

Expected: `OK: current_date injected`

**Step 4: Commit**

```bash
git add code/python/reasoning/prompts/analyst.py
git commit -m "fix(analyst): inject current_date to prevent year misinterpretation (Issue #3)"
```

---

### Task 2: Language consistency guardrail (T3)

**Files:**
- Modify: `config/prompts.xml` (SummarizeResultsPrompt + RankingPrompt)

**Resolves:** Issue #4-D (summary switching to English)

**Step 1: Read current SummarizeResultsPrompt**

Read `config/prompts.xml` lines 417-459.

**Step 2: Replace language instruction in SummarizeResultsPrompt**

Find the line:
```
IMPORTANT: Respond in the same language as the user's question. If the question is in Chinese, respond in Chinese. If in English, respond in English.
```

Replace with:
```
IMPORTANT: 必須使用台灣用語的繁體中文回應。所有摘要、分析、結論都必須以繁體中文撰寫，不得切換為英文。
```

**Step 3: Update RankingPrompt description field**

Read `config/prompts.xml` lines 244-266 (RankingPrompt). In the returnStruc, the `description` field instruction should add:

After the existing description instruction, add:
```
IMPORTANT: description 必須使用繁體中文撰寫。
```

**Step 4: Update RankingPromptForGenerate**

Read `config/prompts.xml` lines 268-340. Find any language-related instruction and add the same zh-TW constraint.

**Step 5: Verify**

```bash
cd /c/users/user/nlweb/code/python
python -c "
from core.prompts import find_prompt, init_prompts
init_prompts()
p, _ = find_prompt('nlweb_news', 'NewsArticle', 'SummarizeResultsPrompt')
assert '繁體中文' in p, 'SummarizeResultsPrompt missing zh-TW'
print('OK: language guardrail added')
"
```

Expected: `OK: language guardrail added`

**Step 6: Commit**

```bash
git add config/prompts.xml
git commit -m "fix(prompts): enforce zh-TW in all response prompts (Issue #4-D)"
```

---

### Task 3: SummarizeResults time gap awareness (T4)

**Files:**
- Modify: `config/prompts.xml` (SummarizeResultsPrompt)

**Resolves:** Issue #6 (summary doesn't mention time discrepancy)

**Step 1: Add temporal context to SummarizeResultsPrompt**

In the SummarizeResultsPrompt (already edited in Task 2), add after the language instruction:

```
{system.temporal_range}

TIME AWARENESS INSTRUCTION:
如果上方有時間範圍資訊，請比對搜尋結果中文章的實際發布日期。
若文章日期與使用者要求的時間範圍明顯不符（例如使用者問「今日」但文章是上個月的），
你必須在摘要開頭明確告知：「目前資料庫中沒有符合指定時間的新聞，以下是最近的相關報導。」
不可假裝搜尋結果符合使用者的時間需求。
```

**Step 2: Verify variable resolution**

```bash
cd /c/users/user/nlweb/code/python
python -c "
from core.prompts import find_prompt, init_prompts
init_prompts()
p, _ = find_prompt('nlweb_news', 'NewsArticle', 'SummarizeResultsPrompt')
assert '{system.temporal_range}' in p or 'temporal_range' in p, 'Missing temporal variable'
assert '時間範圍' in p, 'Missing time awareness instruction'
print('OK: time gap awareness added')
"
```

Note: `{system.temporal_range}` will be in the raw template. After `fill_prompt()`, it resolves to the actual time range string (or empty string if no temporal query).

**Step 3: Commit**

```bash
git add config/prompts.xml
git commit -m "fix(prompts): add time gap awareness to SummarizeResults (Issue #6)"
```

---

### Batch 1 Verification

Start the server and test these queries manually:

1. **Issue #3 (Analyst date):** Deep Research → "台灣在12月中，政府有對治安政策有新的論述或是規劃嗎" → Check report doesn't say "2026年12月"
2. **Issue #4-D (Language):** News Search → Any query → Summary should be in zh-TW, click again → still zh-TW
3. **Issue #6 (Time gap):** News Search → "今日的股市趨勢" → Summary should mention if results don't match today's date

**Report results and wait for feedback before proceeding to Batch 2.**

---

## Batch 2: QueryUnderstanding Unification (T1)

This is the core architectural refactor. 5 sub-tasks.

### Task 4: Add QueryUnderstanding prompt to prompts.xml

**Files:**
- Modify: `config/prompts.xml`

**Step 1: Add the prompt**

Add before the closing root tag in `config/prompts.xml`:

```xml
<Prompt ref="QueryUnderstanding">
  <promptString>
你是新聞搜尋查詢分析助手。分析以下查詢，提取所有結構化資訊。

今天日期：{system.current_date}

使用者查詢：「{request.query}」

{system.query_analysis_hints}

請執行以下所有分析：

## 1. 查詢擴展 (Query Expansion)
判斷查詢是否需要擴展為更具體的新聞搜尋詞。
- 已經具體的查詢（含特定人名+事件）→ 不擴展
- 抽象或廣泛的查詢 → 生成 2-4 個具體的新聞用語搜尋詞
- 擴展詞必須是記者實際會使用的用語，不是抽象同義詞
- 不要包含原始查詢本身
- 使用與原始查詢相同的語言

## 2. 呈現指令分離 (Display Instruction)
若查詢包含呈現指令（條列、比較、整理、歸納、排名、列出...），從搜尋意圖中分離出來。
- 「條列出12月十大新聞」→ display_instruction: "條列出十大", 搜尋意圖: "12月重要新聞"
- 純搜尋查詢 → display_instruction: null

## 3. 作者/記者偵測 (Author Detection)
判斷使用者是否在搜尋特定記者或作者的文章。
- 提到人名 + 記者/作者/編輯（任何順序、任何標點）→ 提取純名字
- 提到人名 + 「的文章」「寫過」「報導」→ 提取純名字
- 「媒體名 的 人名」→ 提取人名
- 純名字只保留中文名 2-4 字，不帶職稱（記者、副總編輯等）
- 英文名保留全名（First Last）
- 如果不確定，回傳 detected: false

## 4. 時間範圍偵測 (Time Range Detection)
判斷查詢是否包含時間意圖，並解析為精確日期範圍。
- 今天日期為 {system.current_date}，所有相對時間以此為基準
- 「12月中」且現在是2月 → 去年12月（2025-12-10 ~ 2025-12-20）
- 「最近一個月」→ 過去30天
- 「2025 9月之前」→ end_date = 2025-08-31
- 如果查詢沒有時間意圖 → detected: false
- confidence: 0.0-1.0（明確日期=1.0，推測=0.7-0.9）

## 5. 主題/領域偵測 (Domain/Topic Detection)
識別查詢的核心主題，提取可用於搜尋結果加分的關鍵字。
- 「政府治安政策」→ primary_topic: "治安", boost_keywords: ["治安", "犯罪", "刑案", "公共安全"]
- 理解語意：「政府」在此是修飾詞，核心是「治安」
- boost_keywords 應包含新聞文章中可能出現的具體用語
- 如果查詢已經足夠具體（如人名搜尋）→ detected: false
  </promptString>
  <returnStruc>
    {
      "rewritten_queries": ["expanded query 1", "expanded query 2"],
      "display_instruction": "呈現指令文字 or null",
      "author": {
        "detected": "true or false",
        "name": "純名字 or null"
      },
      "time_range": {
        "detected": "true or false",
        "start_date": "YYYY-MM-DD or null",
        "end_date": "YYYY-MM-DD or null",
        "confidence": "0.0-1.0"
      },
      "domain": {
        "detected": "true or false",
        "primary_topic": "主題名稱 or null",
        "boost_keywords": ["keyword1", "keyword2"]
      }
    }
  </returnStruc>
</Prompt>
```

**Step 2: Verify prompt loads**

```bash
cd /c/users/user/nlweb/code/python
python -c "
from core.prompts import find_prompt, init_prompts
init_prompts()
p, s = find_prompt('nlweb_news', 'NewsArticle', 'QueryUnderstanding')
assert p is not None, 'Prompt not found'
assert 'author' in str(s), 'Return structure missing author'
print('OK: QueryUnderstanding prompt loaded')
print(f'Return structure keys: {list(s.keys())}')
"
```

Expected: `OK: QueryUnderstanding prompt loaded`

**Step 3: Commit**

```bash
git add config/prompts.xml
git commit -m "feat(prompts): add QueryUnderstanding unified prompt"
```

---

### Task 5: Create QueryUnderstanding module

**Files:**
- Create: `code/python/core/query_analysis/query_understanding.py`

**Step 1: Create the module**

```python
"""
QueryUnderstanding - Unified query analysis module.

Consolidates: QueryRewrite + AuthorIntentDetector + TimeRangeExtractor (LLM) + Domain detection.
Single LLM call after regex fast path.

Resolves Issues: #1, #2, #3, #7, #8, #9, #10
"""

import re
import json
from datetime import datetime, timedelta
from typing import Any, Dict, List, Optional, Tuple

from core.prompts import PromptRunner, fill_prompt
from core.llm import ask_llm
from misc.logger.logging_config_helper import get_configured_logger

logger = get_configured_logger("query_understanding")


class QueryUnderstanding(PromptRunner):
    """
    Unified query understanding with regex fast path + LLM analysis.

    Flow:
    1. Regex fast path (time + author) — runs immediately
    2. Wait for Decontextualize
    3. Single LLM call — fills in everything regex missed
    4. Set handler attributes (backward compatible)
    """

    PROMPT_NAME = "QueryUnderstanding"
    STEP_NAME = "QueryUnderstanding"

    # --- Regex fast path patterns ---

    # Time patterns (from TimeRangeExtractor, high-confidence only)
    TIME_PATTERNS = {
        'yyyy_mm': r'(\d{4})年(\d{1,2})月',
        'today_zh': r'今[天日]',
        'yesterday_zh': r'昨[天日]',
        'this_week_zh': r'這[一]?[週周]|本[週周]',
        'this_month_zh': r'這個月|本月',
        'recent_n_days': r'最近(\d+)[天日]',
        'recent_n_months': r'最近(\d+)個月',
        'month_period_zh': r'(\d{1,2})月[中初底末]',
    }

    # Author patterns (only high-confidence ones)
    AUTHOR_PATTERNS = {
        'name_before_title_zh': r'([\u4e00-\u9fff]{2,4})\s*(?:記者|作者|編輯|副總編輯|總編輯|主筆)',
        'title_colon_name_zh': r'(?:記者|作者|編輯)[：:]\s*([\u4e00-\u9fff]{2,4})',
        'name_possessive_zh': r'([\u4e00-\u9fff]{2,4})\s*(?:的文章|的報導|寫的|的新聞)',
        'by_author_en': r'(?:articles?|posts?|reports?)\s+by\s+([A-Z][a-z]+(?:\s+[A-Z][a-z]+)+)',
    }

    # Stopwords that should NOT be matched as author names
    AUTHOR_STOPWORDS = {
        '什麼', '怎麼', '如何', '為什麼', '哪些', '這個', '那個', '最新', '最近',
        '今天', '昨天', '本週', '分析', '報導', '新聞', '文章', '發展', '趨勢',
        '技術', '產業', '市場', '公司', '企業', '科技', '經濟', '金融', '政治',
    }

    def __init__(self, handler):
        super().__init__(handler)
        self.handler.state.start_precheck_step(self.STEP_NAME)

    async def do(self):
        """Main entry point — regex fast path then LLM."""
        try:
            # 1. Regex fast path on raw query (before decontextualize)
            raw_query = self.handler.query
            regex_time = self._regex_time(raw_query)
            regex_author = self._regex_author(raw_query)

            logger.info(f"[QU] Regex fast path — time: {regex_time is not None}, author: {regex_author is not None}")

            # 2. Wait for Decontextualize to finish
            await self.handler.state._decon_event.wait()
            query = self.handler.decontextualized_query or self.handler.query

            # 3. Build hints from regex results
            hints = self._build_hints(regex_time, regex_author)

            # 4. Set hints on handler so fill_prompt can resolve {system.query_analysis_hints}
            self.handler.query_analysis_hints = hints

            # 5. Call LLM via PromptRunner
            response = await self.run_prompt(
                self.PROMPT_NAME,
                level="low",
                timeout=15,
                max_length=1024
            )

            if not response:
                logger.warning("[QU] LLM returned empty response, using regex-only results")
                response = {}

            # 6. Merge regex + LLM results and set handler attributes
            self._set_handler_attributes(response, regex_time, regex_author)

            logger.info(f"[QU] Done — rewritten_queries={len(self.handler.rewritten_queries)}, "
                        f"temporal={getattr(self.handler, 'temporal_range', {}).get('is_temporal', False)}, "
                        f"author={getattr(self.handler, 'author_search', {}).get('is_author_search', False)}, "
                        f"domain={getattr(self.handler, 'domain_context', {}).get('detected', False)}")

        except Exception as e:
            logger.error(f"[QU] Failed: {e}", exc_info=True)
            # Fallback: set safe defaults so downstream doesn't break
            self._set_defaults()

        finally:
            await self.handler.state.precheck_step_done(self.STEP_NAME)

    # --- Regex Fast Path ---

    def _regex_time(self, query: str) -> Optional[Dict]:
        """Try to extract time range via regex. Returns dict or None."""
        today = datetime.now()

        # Today/yesterday
        if re.search(self.TIME_PATTERNS['today_zh'], query):
            d = today.strftime('%Y-%m-%d')
            return {'is_temporal': True, 'start_date': d, 'end_date': d,
                    'method': 'regex', 'confidence': 1.0}

        if re.search(self.TIME_PATTERNS['yesterday_zh'], query):
            d = (today - timedelta(days=1)).strftime('%Y-%m-%d')
            return {'is_temporal': True, 'start_date': d, 'end_date': d,
                    'method': 'regex', 'confidence': 1.0}

        # This week/month
        if re.search(self.TIME_PATTERNS['this_week_zh'], query):
            start = (today - timedelta(days=today.weekday())).strftime('%Y-%m-%d')
            return {'is_temporal': True, 'start_date': start, 'end_date': today.strftime('%Y-%m-%d'),
                    'method': 'regex', 'confidence': 0.9, 'relative_days': 7}

        if re.search(self.TIME_PATTERNS['this_month_zh'], query):
            start = today.replace(day=1).strftime('%Y-%m-%d')
            return {'is_temporal': True, 'start_date': start, 'end_date': today.strftime('%Y-%m-%d'),
                    'method': 'regex', 'confidence': 0.9, 'relative_days': 30}

        # Recent N days/months
        m = re.search(self.TIME_PATTERNS['recent_n_days'], query)
        if m:
            days = int(m.group(1))
            start = (today - timedelta(days=days)).strftime('%Y-%m-%d')
            return {'is_temporal': True, 'start_date': start, 'end_date': today.strftime('%Y-%m-%d'),
                    'method': 'regex', 'confidence': 0.95, 'relative_days': days}

        m = re.search(self.TIME_PATTERNS['recent_n_months'], query)
        if m:
            months = int(m.group(1))
            start = (today - timedelta(days=months * 30)).strftime('%Y-%m-%d')
            return {'is_temporal': True, 'start_date': start, 'end_date': today.strftime('%Y-%m-%d'),
                    'method': 'regex', 'confidence': 0.9, 'relative_days': months * 30}

        # YYYY年MM月
        m = re.search(self.TIME_PATTERNS['yyyy_mm'], query)
        if m:
            year, month = int(m.group(1)), int(m.group(2))
            if 1 <= month <= 12 and 2000 <= year <= today.year + 1:
                import calendar
                last_day = calendar.monthrange(year, month)[1]
                return {'is_temporal': True, 'start_date': f'{year}-{month:02d}-01',
                        'end_date': f'{year}-{month:02d}-{last_day}',
                        'method': 'regex', 'confidence': 1.0}

        # X月中/初/底 (without year — needs LLM for year inference)
        m = re.search(self.TIME_PATTERNS['month_period_zh'], query)
        if m:
            # Matched but year is ambiguous — let LLM handle
            return None

        return None

    def _regex_author(self, query: str) -> Optional[Dict]:
        """Try to extract author name via regex. Returns dict or None."""
        for pattern_name, pattern in self.AUTHOR_PATTERNS.items():
            m = re.search(pattern, query)
            if m:
                name = m.group(1).strip()
                if name in self.AUTHOR_STOPWORDS or len(name) < 2:
                    continue
                return {'is_author_search': True, 'author_name': name,
                        'pattern_matched': pattern_name}
        return None

    # --- Hint Building ---

    def _build_hints(self, regex_time: Optional[Dict], regex_author: Optional[Dict]) -> str:
        """Build hint text for LLM from regex results."""
        parts = []
        if regex_time:
            parts.append(f"Regex 時間偵測結果：{json.dumps(regex_time, ensure_ascii=False)}")
            parts.append("（此結果 confidence 高，你可以直接採用，除非你認為 regex 解析有誤）")
        if regex_author:
            parts.append(f"Regex 作者偵測結果：{json.dumps(regex_author, ensure_ascii=False)}")
            parts.append("（此結果 confidence 高，你可以直接採用）")
        if not parts:
            parts.append("Regex 預分析：無高信度結果，請完整分析所有欄位。")
        return "\n".join(parts)

    # --- Handler Attribute Setting ---

    def _set_handler_attributes(self, llm_response: Dict, regex_time: Optional[Dict], regex_author: Optional[Dict]):
        """Merge regex + LLM results and set handler attributes for backward compatibility."""

        # --- rewritten_queries ---
        raw_queries = llm_response.get('rewritten_queries', [])
        if isinstance(raw_queries, list):
            self.handler.rewritten_queries = [q for q in raw_queries if isinstance(q, str) and q.strip()]
        else:
            self.handler.rewritten_queries = []
        self.handler.needs_query_expansion = len(self.handler.rewritten_queries) > 0

        # --- display_instruction ---
        self.handler.display_instruction = llm_response.get('display_instruction') or None

        # --- temporal_range (regex wins if confident, else LLM) ---
        if regex_time and regex_time.get('confidence', 0) >= 0.9:
            self.handler.temporal_range = regex_time
        else:
            llm_time = llm_response.get('time_range', {})
            detected = str(llm_time.get('detected', 'false')).lower() == 'true'
            if detected and llm_time.get('start_date'):
                confidence = float(llm_time.get('confidence', 0.8))
                self.handler.temporal_range = {
                    'is_temporal': True,
                    'start_date': llm_time['start_date'],
                    'end_date': llm_time.get('end_date'),
                    'method': 'llm',
                    'confidence': confidence,
                    'relative_days': None,
                }
            elif regex_time:
                # LLM didn't detect but regex had something — use regex
                self.handler.temporal_range = regex_time
            else:
                self.handler.temporal_range = {'is_temporal': False}

        # --- author_search (regex wins if matched, else LLM) ---
        if regex_author:
            self.handler.author_search = regex_author
        else:
            llm_author = llm_response.get('author', {})
            detected = str(llm_author.get('detected', 'false')).lower() == 'true'
            if detected and llm_author.get('name'):
                self.handler.author_search = {
                    'is_author_search': True,
                    'author_name': llm_author['name'],
                    'pattern_matched': 'llm'
                }
            else:
                self.handler.author_search = {'is_author_search': False}

        # --- domain_context (always from LLM) ---
        llm_domain = llm_response.get('domain', {})
        detected = str(llm_domain.get('detected', 'false')).lower() == 'true'
        if detected:
            self.handler.domain_context = {
                'detected': True,
                'primary_topic': llm_domain.get('primary_topic', ''),
                'boost_keywords': llm_domain.get('boost_keywords', []),
            }
        else:
            self.handler.domain_context = {'detected': False, 'boost_keywords': []}

    def _set_defaults(self):
        """Set safe defaults when everything fails."""
        self.handler.rewritten_queries = []
        self.handler.needs_query_expansion = False
        self.handler.display_instruction = None
        self.handler.temporal_range = {'is_temporal': False}
        self.handler.author_search = {'is_author_search': False}
        self.handler.domain_context = {'detected': False, 'boost_keywords': []}
```

**Step 2: Add `system.query_analysis_hints` variable resolution**

In `core/prompts.py`, find the `get_prompt_variable_value()` function. Add a new case for `system.query_analysis_hints`:

```python
elif variable == "system.query_analysis_hints":
    value = getattr(handler, 'query_analysis_hints', '')
```

Add this alongside the existing `system.temporal_range` and `system.temporal_constraint` cases.

**Step 3: Verify module imports**

```bash
cd /c/users/user/nlweb/code/python
python -c "from core.query_analysis.query_understanding import QueryUnderstanding; print('OK: module imports')"
```

Expected: `OK: module imports`

**Step 4: Commit**

```bash
git add code/python/core/query_analysis/query_understanding.py code/python/core/prompts.py
git commit -m "feat(query): create unified QueryUnderstanding module with regex + LLM"
```

---

### Task 6: Wire QueryUnderstanding into baseHandler.py

**Files:**
- Modify: `code/python/core/baseHandler.py:350-394`

**Step 1: Read current prepare() method**

Read `core/baseHandler.py` lines 350-394 to see current task list.

**Step 2: Replace 3 tasks with 1**

In the `prepare()` method, replace:

```python
tasks.append(asyncio.create_task(query_rewrite.QueryRewrite(self).do()))
tasks.append(asyncio.create_task(time_range_extractor.TimeRangeExtractor(self).do()))
tasks.append(asyncio.create_task(author_intent_detector.AuthorIntentDetector(self).do()))
```

With:

```python
tasks.append(asyncio.create_task(query_understanding.QueryUnderstanding(self).do()))
```

Also add the import at the top of `baseHandler.py`:

```python
from core.query_analysis import query_understanding
```

And remove (or comment out) the old imports:

```python
# from core.query_analysis import query_rewrite        # Replaced by query_understanding
# from core.query_analysis import time_range_extractor  # Replaced by query_understanding
# from core.query_analysis import author_intent_detector # Replaced by query_understanding
```

**Step 3: Verify server starts**

```bash
cd /c/users/user/nlweb/code/python
python -c "from core.baseHandler import NLWebHandler; print('OK: baseHandler imports')"
```

**Step 4: Commit**

```bash
git add code/python/core/baseHandler.py
git commit -m "refactor(handler): replace 3 pre-checks with unified QueryUnderstanding"
```

---

### Task 7: Refactor qdrant.py — remove hardcoded domain analysis

**Files:**
- Modify: `retrieval_providers/qdrant.py:884-1137`

This is the most delicate change. Read carefully before modifying.

**Step 1: Read current domain detection code**

Read `retrieval_providers/qdrant.py` lines 882-900 (domain_indicators, domain_entities).

**Step 2: Replace hardcoded domain detection with handler attribute**

Replace the hardcoded domain detection block (lines ~884-898):

```python
# OLD: hardcoded domain lists
domain_indicators = ['零售', '金融', '製造', ...]
domain_entities = { '零售': ['walmart', ...], ... }
query_domains = [domain for domain in domain_indicators if domain in query]
```

With:

```python
# NEW: read domain context from QueryUnderstanding
domain_context = getattr(handler, 'domain_context', {}) if handler else {}
boost_keywords = domain_context.get('boost_keywords', [])
```

Note: `handler` reference — check how the current code accesses the handler. It may be `self.handler` or passed as parameter. Trace through the code to confirm.

**Step 3: Replace on_topic/off_topic filtering with boost scoring**

Find the block around lines 1121-1137:

```python
# OLD: hard filter
if query_domains and has_critical_keyword:
    on_topic_results.append((final_score, point))
elif query_domains and not has_critical_keyword:
    off_topic_results.append((final_score, point))
else:
    scored_results.append((final_score, point))

# ...
scored_results = on_topic_results  # ONLY on-topic!
```

Replace with:

```python
# NEW: domain boost (all results kept)
domain_boost = 0.0
if boost_keywords:
    for kw in boost_keywords:
        kw_lower = kw.lower()
        if kw_lower in name:
            domain_boost += 0.3  # Title match — strong boost
        elif kw_lower in schema_json:
            domain_boost += 0.1  # Body match — moderate boost

final_score = final_score + domain_boost
scored_results.append((final_score, point))
```

Remove the `on_topic_results`, `off_topic_results` variables and the combining logic at lines 1130-1137.

Also remove the `has_critical_keyword` computation block (~lines 980-1025) and the `negative_indicators` dict (~lines 986-994).

**Step 4: Replace hardcoded temporal keywords**

Find lines ~1069-1072:

```python
# OLD: hardcoded temporal keywords
temporal_keywords = ['最新', '最近', '近期', 'latest', 'recent', '新', '現在', '目前', '當前']
is_temporal_query = any(keyword in query for keyword in temporal_keywords)
```

Replace with:

```python
# NEW: use handler's temporal_range from QueryUnderstanding
temporal_range = getattr(handler, 'temporal_range', {}) if handler else {}
is_temporal_query = temporal_range.get('is_temporal', False)
```

**Step 5: Verify**

```bash
cd /c/users/user/nlweb/code/python
python -c "from retrieval_providers.qdrant import QdrantRetriever; print('OK: qdrant imports')"
```

**Step 6: Commit**

```bash
git add retrieval_providers/qdrant.py
git commit -m "refactor(qdrant): replace hardcoded domain filter with LLM-based boost scoring"
```

---

### Task 8: Integration test — full search pipeline

**Verification:** Start server and test these queries:

1. **Domain boost (Issue #2):** "台灣在12月中，政府有對治安政策有新的論述或是規劃嗎"
   - Should return security/crime articles, NOT just articles with "政府" in title
   - Check logs for `[QU]` entries showing domain: primary_topic = "治安"

2. **Author detection (Issue #7):** "王宏仁記者有什麼文章?"
   - Check logs for `[QU]` entries showing author: name = "王宏仁"
   - Results should contain articles by 王宏仁

3. **Time range (Issue #3):** "12月中的新聞"
   - Check logs for temporal_range with 2025-12 dates (not 2026-12)

4. **Query expansion (Issue #1):** "條列出12月十大新聞"
   - Check logs for display_instruction = "條列出十大"
   - rewritten_queries should contain news-specific terms

5. **No regression — simple query:** "台積電法說會"
   - Should work as before, no expansion needed

**Report results and wait for feedback before proceeding to Batch 3.**

---

## Batch 3: Frontend State Management (T2)

### Task 9: Add clearQueryState() and fix notification persistence

**Files:**
- Modify: `static/news-search.js`

**Step 1: Read current state**

Read `static/news-search.js`:
- Lines 153-157 (global variables)
- Lines 1062-1075 (`showTimeFilterRelaxedWarning`)
- Lines 1584-1600 (`performSearch` start)

**Step 2: Create clearQueryState() function**

Add after the `cancelAllActiveRequests()` function (around line 1555):

```javascript
// Clear all query-related UI state before starting a new search
function clearQueryState() {
    // Clear deep research state
    currentResearchReport = null;
    currentArgumentGraph = null;
    currentChainAnalysis = null;

    // Clear time filter warning
    const timeWarning = document.getElementById('timeFilterWarning');
    if (timeWarning) timeWarning.remove();

    // Clear any existing summary content
    const summaryContainer = document.getElementById('aiSummary');
    if (summaryContainer) summaryContainer.innerHTML = '';

    // Clear research report container
    const reportContainer = document.getElementById('researchReportContent');
    if (reportContainer) reportContainer.innerHTML = '';

    // Clear knowledge graph container
    const kgContainer = document.getElementById('knowledgeGraphContainer');
    if (kgContainer) kgContainer.innerHTML = '';

    // Clear reasoning chain container
    const chainContainer = document.getElementById('reasoningChainContainer');
    if (chainContainer) chainContainer.innerHTML = '';
}
```

Note: Verify the exact container element IDs by reading the HTML. The IDs above are educated guesses — confirm them from the actual HTML structure before implementing.

**Step 3: Call clearQueryState() in performSearch()**

In `performSearch()` (line ~1588), right after `cancelAllActiveRequests()`:

```javascript
cancelAllActiveRequests();
clearQueryState();  // <-- ADD THIS
setProcessingState(true);
```

**Step 4: Also clear on new conversation**

Find the function that starts a new conversation / clears chat history. Add `clearQueryState()` there too.

**Step 5: Verify manually**

1. Search for "WBC 經典賽" → get results
2. Switch to Deep Research → "李昀璇記者是誰" → should show fresh results, NOT previous search
3. Search again with time query → notification appears → search again without time → notification should be gone

**Step 6: Commit**

```bash
git add static/news-search.js
git commit -m "fix(frontend): clear all UI state on new search/conversation (Issues #4-C, #5-C, #11)"
```

---

## Final Verification Checklist

| Issue | Query to Test | Expected Behavior | Task |
|-------|--------------|-------------------|------|
| #1 | 「條列出12月十大新聞」 | display_instruction separated, search results are about Dec news | T1 |
| #2 | 「政府治安政策」 | Returns security articles, not just "政府" title matches | T1 |
| #3 | 「12月中...」(deep research) | Report says 2025年12月, not 2026 | T1 + T5 |
| #4-C | Switch between search and deep research | No stale content from previous query | T2 |
| #4-D | Any query, click twice | Summary stays in zh-TW | T3 |
| #5-C | Same as #4-C | Same as #4-C | T2 |
| #6 | 「今日的股市趨勢」 | Summary mentions if results don't match today | T4 |
| #7 | 「iThome記者：郭又華 的文章」 | Author filter extracts "郭又華" | T1 |
| #8 | 「王宏仁記者有什麼文章?」 | Author filter extracts "王宏仁" | T1 |
| #9 | 「王若樸記者在2025 9月之前」 | Author + time both detected | T1 |
| #10 | 「李昀璇記者是誰?」(deep research) | Finds articles by 李昀璇, synthesizes profile | T1 |
| #11 | Search with time limit, then without | Warning notification disappears | T2 |
